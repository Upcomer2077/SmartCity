[project]
name = "simulator"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = ["python-dotenv>=1.2.2"]

[tool.uv.workspace]
members = ["common", "emitter", "transmitter", "collector"]

[dependency-groups]
dev = []
