[project]
name = "broker"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "aiosqlite>=0.22.0",
    "kafka-python-ng>=2.2.3",
    "sqlalchemy>=2.0.45",
    "common",
]

[tool.uv.sources]
common = { workspace = true }

[project.scripts]
broker = "broker:main"
