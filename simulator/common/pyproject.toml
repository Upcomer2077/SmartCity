[project]
name = "common"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Eugene", email = "evgenijgoncarevic@gmail.com" }]
requires-python = ">=3.14"
dependencies = ["aiosqlite>=0.22.0", "sqlalchemy>=2.0.45"]


[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = ["alembic>=1.18.4"]
