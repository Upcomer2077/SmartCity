[project]
name = "transmitter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["aiokafka>=0.14.0", "common", "protobuf>=6.33.4"]

[tool.uv.sources]
common = { workspace = true }
