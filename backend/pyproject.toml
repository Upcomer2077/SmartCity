[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = ["protobuf>=6.33.5", "sqlalchemy>=2.0.46"]

[dependency-groups]
dev = ["alembic>=1.18.3"]

[tool.uv.workspace]
members = [
    "consumer",
    "common",
    "app",
]
