# 🏙️ Техническое задание

## Проект: SmartCity
Интеллектуальная платформа мониторинга городской инфраструктуры

## 1. Общие сведения

### Название проекта: SmartCity
### Цель разработки:
Создание веб-платформы, собирающей, обрабатывающей и визуализирующей данные от IoT-датчиков, установленных в городской инфраструктуре (транспорт, экология, температура, освещение и т. д.).

### Задачи системы:

* Принимать и обрабатывать потоковые данные от датчиков в реальном времени;

* Агрегировать и хранить данные в аналитической БД;

* Предоставлять REST и WebSocket API для визуализации;

* Отображать данные и прогнозы в интерактивной панели управления;

* Обеспечивать масштабируемость, тестируемость и мониторинг всех компонентов.

<!-- ### Полное описание системы (Data Flow):

### Сущности:

* Симулятор для имитации работы датчиков
    * Симулятор непосредственно
    - Брокер для пересылки
* Сервер  -->

## 2. Архитектура системы

Система состоит из следующих подсистем:

### 2.1. Frontend

* Разработан на NuxtJS 3 (TypeScript).

* Использует Bootstrap для UI и адаптивности.

* Реализует JWT-аутентификацию (доступ к API по токенам).

* Использует cookies для хранения refresh-токена.

* Реализует WebSocket-подключение для отображения данных в реальном времени.

### Отображает:

* карту города с активными сенсорами;

* графики и статистику;

* прогнозы (температура, загруженность, качество воздуха);

* панель администратора для управления датчиками.

### 2.2. Backend

#### Основной фреймворк — FastAPI (Python 3.11).

### Использует:

* SQLAlchemy — ORM и слой доступа к данным;

* PostgreSQL — основное хранилище данных;

* Redis — кеш и очередь короткоживущих данных;

* Kafka — потоковая шина данных от IoT-датчиков;

* gRPC — взаимодействие с сервисом аналитики (модуль прогнозирования);

* WebSocket — отправка данных в реальном времени фронтенду;

* Docker + Nginx — контейнеризация и маршрутизация;

* pydantic — строгая валидация моделей данных.

### 2.3. Модуль имитации IoT-датчиков

#### Отдельный микросервис на Python (python-kafka).

#### Генерирует данные:

* ID сенсора;

* тип данных (traffic, temperature, air_quality, light);

* значение (float);

* координаты и метку времени.

* Отправляет данные в Kafka-топики раз в 1–3 секунды.

### 2.4. Аналитический модуль (gRPC-сервис)

#### Получает данные от FastAPI;

#### Использует базовую ML-модель для прогнозирования показателей (например, изменение температуры или загрязнения);

#### Возвращает результаты в API.

### 2.5. DevOps и инфраструктура

* CI/CD через GitHub Actions;

* Развёртывание микросервисов в Kubernetes;

* Контейнеризация в Docker;

* Prometheus — сбор метрик;

* Grafana + Loki — визуализация и алерты;

* Nginx — балансировка запросов.

## 3. Функциональные требования
### 3.1. Пользовательские роли
Роль	| Возможности |
| ----------|---------|
Гость	| Просмотр общих дашбордов (без авторизации) |
Администратор |	Управление пользователями, настройка датчиков, просмотр логов |

### 3.2. Основные функции

* Авторизация (JWT) (Для администраторов).

* Отображение данных с датчиков на карте.

* Обновление данных в реальном времени (WebSocket).

* Агрегирование и хранение исторических данных.

* Отчётность и прогнозирование (через gRPC-сервис).

* Система уведомлений о превышении пороговых значений (через Kafka + Redis).

* Панель администратора:

* управление датчиками;

* управление администраторами

## 4. Нефункциональные требования
| Параметр |	Требование |
| ----------|---------|
Производительность	| поддержка до 10 000 сообщений/сек из Kafka
Масштабируемость	| горизонтальное масштабирование микросервисов
Безопасность |	JWT, HTTPS, защита от XSS/CSRF
Доступность |	99.5% uptime
Мониторинг |	метрики Prometheus, алерты Grafana
CI/CD |	полная автоматизация сборки, тестов и деплоя

## 5. Тестирование
### 5.1. Frontend

| Тип теста | Инструмент	 | Цель | 
| ----------|---------| ------- |
Unit	| Jest + Vue Test| Utils	Проверка логики компонентов|
E2E|	Cypress|	Проверка пользовательских сценариев
UI Snapshot	| Storybook + Chromatic |	Контроль изменений интерфейса
Accessibility |	axe-core	| Проверка доступности интерфейса
Static Analysis |	ESLint + Prettier |	Проверка качества кода

### 5.2. Backend
| Тип теста | Инструмент	 | Цель | 
| ----------|---------| ------- |
| Unit	| Pytest	| Проверка API и бизнес-логики
Integration |	Docker Compose + Pytest |	Проверка работы связок FastAPI ↔ DB ↔ Kafka
Load	 | Locust |	Измерение производительности
API |	Postman/Newman |	Тестирование REST-эндпоинтов
Security |	OWASP ZAP (вручную) |	Проверка безопасности API
Static Analysis |	mypy + flake8 |	Проверка стиля и типов
### 5.3. DevOps тесты

* Проверка CI/CD пайплайна (успешная сборка и деплой).

* Smoke-тесты контейнеров после деплоя в K8s.

* Мониторинг доступности сервисов через Prometheus.

## CI/CD процесс

* При push в ветку main → запускается GitHub Actions pipeline:

*  установка зависимостей;

* запуск unit и integration тестов;

* проверка линтинга;

* сборка Docker-образов;

* деплой в Kubernetes кластер;

* smoke-тесты и уведомление о статусе.

* При merge PR → автосборка preview-среды для тестирования.
